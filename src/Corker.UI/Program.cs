using Corker.UI;

public class Program
{
    public static void Main(string[] args)
    {
        var builder = MauiProgram.CreateMauiApp();
        // This is a stub entry point for the net9.0 Linux verification build.
        // It won't actually run a UI on Linux without a specific head (like Blazor Server or GTK).
        // But it allows the project to compile to verify logic.
    }
}
